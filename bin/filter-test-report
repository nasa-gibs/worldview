#!/usr/bin/env python

import sys

filtered = True
for line in sys.stdin:
    if line.startswith("<?xml"):
        filtered = False
    if line.startswith("</testsuites>"):
        filtered = True
    if not filtered:
        sys.stdout.write(line)
