import React from 'react';

const VectorAlertModalBody = () => (
  <div className="event-alert-modal-body">
    <h3 className="wv-data-unavailable-header">
      Why can’t I click on a feature in a vector layer?
    </h3>
    <p>
      Some vector layers require zooming in order to click.
    </p>
  </div>
);

export default VectorAlertModalBody;
