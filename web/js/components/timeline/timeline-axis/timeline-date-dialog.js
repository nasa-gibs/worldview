import React from 'react';
import PropTypes from 'prop-types';

class TimelineDateDialog extends React.Component {
  constructor(props) {
    super(props);
    this.state = { };
  }

  render() {
    return (
<div></div>
    );
  };
}

TimelineDateDialog.defaultProps = {
};
TimelineDateDialog.propTypes = {
};

export default TimelineDateDialog;
